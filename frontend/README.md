## Cue Frontend

Vite + React prototype for the Cue phone-frame experience.

### Setup

```bash
cp .env.example .env
npm install
npm run dev
```

The frontend expects the backend at `VITE_API_BASE_URL`, defaulting to `http://localhost:8787/api`.
